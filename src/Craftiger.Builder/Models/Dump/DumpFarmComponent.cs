namespace Craftiger.Builder.Models.Dump;

/// <summary>One tiered Industrial Farm component block variant with its Java class and tier.</summary>
public sealed record DumpFarmComponent(string ComponentClass, int Tier);
