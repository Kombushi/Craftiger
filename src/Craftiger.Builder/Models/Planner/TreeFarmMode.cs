namespace Craftiger.Builder.Models.Planner;

/// <summary>The Tree Growth Simulator's output classes, each harvested by its own tool; the order is the output slot the dump lists them in.</summary>
public enum TreeFarmMode
{
    Log,
    Sapling,
    Leaves,
    Fruit,
}
