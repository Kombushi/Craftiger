namespace Craftiger.Solver.Models.Factory;

/// <summary>Where an auto-infinite primitive comes from; mob drops count only when the factory farms mobs.</summary>
public enum SeedKind
{
    World,
    Farm,
    Mob,
}
